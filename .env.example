# Supabase project that hosts the Edge Functions (the backend) and the waitlist
# table. Both values are public and safe to ship in the browser bundle.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
